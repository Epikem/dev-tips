{
  "name": "ts",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "pnpm clean && pnpm build && pnpm copy-public-files && pnpm start-server",
    "clean": "rm -r ./dist",
    "build": "tsc",
    "copy-public-files": "cp -r ./public/* ./dist",
    "start-server": "http-server ./dist",
    "dev": "webpack-dev-server"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "http-server": "^0.12.3",
    "ts-loader": "^9.1.2",
    "tsconfig-paths-webpack-plugin": "^3.5.1",
    "typescript": "^4.2.4",
    "webpack": "^5.37.0"
  },
  "devDependencies": {
    "webpack-cli": "^4.7.0",
    "webpack-dev-server": "^3.11.2"
  }
}
